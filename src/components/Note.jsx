import React from 'react'


const Note = () => {
    if(note.important)
    {
        return (<li>{'note.content'}</li>);
    }else{
        return (<li>{'.'}{'note.content'}</li>);
    }
  
}

export default Note
